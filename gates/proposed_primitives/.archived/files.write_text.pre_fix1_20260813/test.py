import unittest\nimport tempfile\nimport os\nfrom friday.l1.files import write_notes\n\nclass TestWriteNotes(unittest.TestCase):\n    def test_write_notes(self):\n        with tempfile.TemporaryDirectory() as tmpdir:\n            notes_path = os.path.join(tmpdir, "notes.txt")\n            text = "Hello world"\n            result = write_notes(notes_path, text)\n            self.assertTrue(result.startswith("Wrote to"))\n            with open(notes_path, 'r', encoding='utf-8') as f:\n                self.assertEqual(f.read(), text)